<!DOCTYPE html>
<html lang="es">
<!-- Include Head -->
<%- include('../partials/head', {title: 'Registro', image: ''}) %>

<body>
   <!-- Header -->
   <%- include('../partials/headerNavbar') %>
   <!-- /Header -->

   <div class="container products-wrapper">
      <div class="row justify-content-center">
         <div class="col-12">
            <h2 class="products-title"><%= locals.user.email %> </h2>
         </div>
         <div class="col-8">
            <div class="row product-detail justify-content-center">
               <div class="col-8 my-3">
                  <img src="/images/users/<%= locals.user.image %>" alt="<%= locals.user.email %>">
               </div>
            </div>
         </div>
      </div>

   </div>
   <!-- /todos los productos -->

   <!-- footer -->
   <%- include('../partials/footer') %>
   <!-- /footer -->
</body>

</html>